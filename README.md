# ChronoPath
Replay system built for FTC team 19084 ZENITH
use `git pull`, it updates the FTC repo, including the gitlink pointer then
`git submodule update --init --recursive`, it actually fetches/checks out chronopath/ to match that pointer
