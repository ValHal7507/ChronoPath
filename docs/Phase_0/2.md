CONTEXT
ChronoPath — FTC Team Zenith 19084's robot replay/visualization system.
docs/replay-format-spec.md (Phase 0 Task 1) is complete, including §9.1
(gamepad button bitmask constants) and the no-range-validation rule added
in the last revision. It is now the single source of truth for the binary
format. We are on Phase 0, Task 2 of docs/Plan.md — the last task before
any code gets written.

GOAL
Create docs/directory-structure.md describing the two subprojects and how
their format-constant files stay in sync.

EXISTING ARCHITECTURE

- docs/replay-format-spec.md defines a binary format consumed by two
  independent implementations: a Java recorder on the robot and a Kotlin
  Multiplatform parser/UI app.
- Per replay-format-spec.md §9/§9.1, ReplayFormatConstants.java and
  ReplayFormatConstants.kt must stay numerically identical, manually
  mirrored (per Plan.md Task 11), not codegen'd.
- No replay-system code exists yet. (The repo has pre-existing TeamCode
  modules unrelated to this feature — do not reference or restructure
  them in this document.)

FILES YOU MAY MODIFY

- docs/directory-structure.md (create this)

FILES YOU MUST NOT MODIFY

- docs/Plan.md
- docs/AGENTS.md
- docs/FTC_SYNTAX
- docs/replay-format-spec.md
- any existing TeamCode/robot-module file

TECHNICAL REQUIREMENTS
Document must cover:

1. The two subproject roots (robot-module/, replay-app/) and their
   top-level purpose in one line each.
2. Where ReplayFormatConstants.java and ReplayFormatConstants.kt will
   live in each subproject's source tree.
3. The sync mechanism: manual mirroring (per Plan.md Task 11) — state
   explicitly that there is no codegen step, and describe the manual
   process for keeping them numerically identical: a comment header in
   both constants files pointing at replay-format-spec.md §9/§9.1, and a
   checklist step ("if you edit one, edit the other, then re-run the
   self-check diff") whenever the spec doc's constant summary changes.
4. High-level directory tree for each subproject as currently planned:
   - robot-module/: OpMode integration point (Phase 1 Task 9), recorder
     class, format/constants package
   - replay-app/: shared/parsing module (Phase 3), platform-specific
     file-reading targets per Plan.md Task 18 (desktop via okio/java.io,
     Android via ContentResolver), Compose UI module (Phase 4-5)
     Present as tree diagrams, not exhaustive file lists — most files don't
     exist yet and shouldn't be invented here.

CONSTRAINTS

- Do not create any source files or folders — documentation only.
- Do not restate byte-level format details from replay-format-spec.md —
  reference it by section number instead (§9, §9.1).
- Do not touch or describe pre-existing TeamCode modules beyond noting
  they're out of scope for this document.

ACCEPTANCE CRITERIA

- Someone opening the repo for the first time can tell from this file
  alone where the two constants files belong and why keeping them in sync
  is a manual, spec-doc-first process rather than automatic.
- No byte offset, size, or scale factor from replay-format-spec.md is
  duplicated here.

TESTS YOU MUST PERFORM

- N/A (documentation-only task, no code to test).

REQUIRED FINAL REPORT

- Path of the file created.
- The two directory trees, reproduced inline in the report.
- Confirmation that no byte-level format details were duplicated from
  replay-format-spec.md (only referenced by section number).
- Confirmation that no existing TeamCode files were touched.
