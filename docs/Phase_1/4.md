CONTEXT
ChronoPath — FTC Team Zenith 19084's robot replay/visualization system.
Phase 0 (spec + directory structure) and Phase 1 Task 3
(ReplayFormatConstants.java) are complete and verified against
docs/replay-format-spec.md §9/§9.1. We are on Phase 1, Task 4 of
docs/Plan.md.

GOAL
Create ReplaySample.java — a plain data holder for one sample's fields in
engineering units. No serialization logic (that's Task 5).

EXISTING ARCHITECTURE

- docs/replay-format-spec.md §5 defines the sample record's field list:
  x, y, headingDeg (floats), dtMs (integer ms), and both gamepads'
  button/stick/trigger state (§5 fields 0–17).
- docs/replay-format-spec.md §5.1 defines the 15 named buttons per
  gamepad as individual bits in a packed uint32 — but that packing is a
  serialization concern (Task 5's toBytes()), not a storage concern.
  ReplaySample stores engineering-unit values, not packed bytes.
- ReplayFormatConstants.java exists at
  robot-module/src/main/java/org/firstinspires/ftc/teamcode/replay/
  and defines BUTTON_A..BUTTON_RIGHT_STICK bit constants — ReplaySample
  does not need these yet (they're for Task 5's bitmask packing), but
  don't contradict their bit meanings in whatever field names you choose.
- docs/directory-structure.md §4.1 places this file at
  robot-module/src/main/java/org/firstinspires/ftc/teamcode/replay/ReplaySample.java

FILES YOU MAY MODIFY

- robot-module/src/main/java/org/firstinspires/ftc/teamcode/replay/ReplaySample.java (create this)

FILES YOU MUST NOT MODIFY

- docs/Plan.md
- docs/AGENTS.md
- docs/FTC_SYNTAX
- docs/replay-format-spec.md
- docs/directory-structure.md
- ReplayFormatConstants.java
- any pre-existing TeamCode file

TECHNICAL REQUIREMENTS
Before writing anything, re-read replay-format-spec.md §5 and §5.1 in
full (per AGENTS.md's "never guess" rule).

ReplaySample must hold, in engineering units, exactly the fields listed
in spec §5:

- float x, float y, float headingDeg — robot pose
- int dtMs (or long, but must be non-negative integer milliseconds per
  spec §1.3/§5) — elapsed ms since previous sample
- gamepad1 and gamepad2 state, each covering all 15 named buttons
  (§5.1) individually and the 6 stick/trigger float axes (§5:
  left_stick_x/y, right_stick_x/y, left_trigger, right_trigger)

Design decision left to you, per AGENTS.md ("if a question isn't
answered by the source-of-truth files, use working judgment for pure
code-structure choices that don't touch byte layout, scale factors, or
API calls — but state the choice in your report"):

- Whether button states are stored as 15 individual booleans per
  gamepad, or as a single int bitmask per gamepad already matching the
  §5.1 bit layout. Either is acceptable since packing to record bytes is
  Task 5's job, not this task's. State which you chose and why.
- Whether gamepad1/gamepad2 are nested static inner classes, a shared
  GamepadState type, or flat fields on ReplaySample directly — pick
  whichever keeps the class simplest for Task 5 to serialize field-by-
  field against spec §5's offset table.

CONSTRAINTS

- No serialization/deserialization code — no toBytes(), no byte[]
  anywhere in this file. That is Task 5.
- No FTC SDK Gamepad type as a field type — store primitive
  boolean/float/int values copied out of a Gamepad, not a live SDK
  object, so this class stays a plain, hardware-independent data holder
  testable without any FTC runtime (per AGENTS.md Testing Expectations,
  Task 6 needs to construct this with hardcoded fixture values).
- Do not add any field not present in spec §5/§5.1 (no turret angle —
  that's the Task 31/Phase 7 optional channel, gated behind
  CHANNEL_TURRET, not part of this task).
- Do not add validation/clamping logic — spec §5 explicitly says
  parsers/recorders must not clamp or range-validate.

ACCEPTANCE CRITERIA

- Every field in spec §5 (record fields 0–17) has a corresponding field
  in ReplaySample, traceable one-to-one.
- File compiles standalone — no dependency on ReplayFormatConstants,
  ReplayRecorder, or any FTC SDK class.
- A Task 6 unit test could construct a ReplaySample with the spec §8.2
  worked-example's engineering-unit values (x=42.2417, y=67.119,
  headingDeg=-12.346, dtMs=17, gamepad1 A+left_bumper held,
  left_stick_x=-0.50, etc.) using only this class's public
  fields/constructor — confirm this mentally before finishing.

TESTS YOU MUST PERFORM

- N/A (no logic to unit test yet — this is a plain data class. Task 6
  writes the first real test, against Task 5's toBytes()).

REQUIRED FINAL REPORT

- Path of the file created.
- The design choice made for button representation (booleans vs
  bitmask) and gamepad grouping (nested class vs flat fields), with
  one-sentence justification each.
- Confirmation every spec §5 field 0–17 is represented, listed field-by-
  field against the spec table.
- Confirmation no byte[], no FTC SDK Gamepad type, and no turret/extra
  field appear anywhere in the file.
