CONTEXT
ChronoPath — FTC Team Zenith 19084's robot replay/visualization system.
Phase 0 is complete: docs/replay-format-spec.md (single source of truth
for the binary format, including §9/§9.1 constants) and
docs/directory-structure.md (where files live, how the two constants
files stay in sync) are both done. We are starting Phase 1, Task 3 of
docs/Plan.md — the first code task in the project.

GOAL
Create ReplayFormatConstants.java containing every constant from
replay-format-spec.md §9 and §9.1, transcribed exactly, with no logic.

EXISTING ARCHITECTURE

- docs/replay-format-spec.md defines the full binary format.
- docs/directory-structure.md §2 and §4.1 specify this file's exact path:
  robot-module/src/main/java/org/firstinspires/ftc/teamcode/replay/ReplayFormatConstants.java
- No other replay-system code exists yet. This is the first file.
- Per directory-structure.md §3.1, this file must begin with the required
  "SYNCED COPY" comment header pointing at spec §9/§9.1.

FILES YOU MAY MODIFY

- robot-module/src/main/java/org/firstinspires/ftc/teamcode/replay/ReplayFormatConstants.java (create this)

FILES YOU MUST NOT MODIFY

- docs/Plan.md
- docs/AGENTS.md
- docs/FTC_SYNTAX
- docs/replay-format-spec.md
- docs/directory-structure.md
- any pre-existing TeamCode file

TECHNICAL REQUIREMENTS
Before writing anything, re-read replay-format-spec.md §9 and §9.1 in
full (per AGENTS.md / Plan.md's "never guess a byte offset" rule) — do
not rely on memory of the earlier conversation.

The file must declare, as `public static final` fields, exactly:

1. Magic bytes — as a byte[] or four individual constants, matching §3
   (0x5A, 0x4E, 0x54, 0x48).
2. VERSION = 1
3. HEADER_SIZE = 16
4. BASE_RECORD_SIZE = 72
5. MODE_AUTONOMOUS = 0, MODE_TELEOP = 1
6. CHANNEL_TURRET = 0x01
7. All 15 gamepad button bitmask constants from §9.1 (BUTTON_A through
   BUTTON_RIGHT_STICK) plus BUTTONS_RESERVED_MASK = 0xFFFF8000
8. Any other constant listed in the §9 summary table not already covered
   above (rounding scale, dtMs scale, etc. — as documentation constants
   or comments if they aren't meaningfully typed values, per spec §9)

Use the exact names given in spec §9/§9.1 — do not rename, abbreviate, or
reorganize them.

Class must be `final` with a private constructor (or otherwise
non-instantiable) — it's a constants holder, per Plan Task 3 ("No
logic").

CONSTRAINTS

- No logic of any kind: no methods beyond a private/no-op constructor, no
  computed fields, no imports beyond what's needed for the byte[]/const
  declarations.
- No serialization, parsing, or validation code — that's Tasks 5+.
- Must begin with the exact "SYNCED COPY" comment header format specified
  in directory-structure.md §3.1.
- Package declaration must be org.firstinspires.ftc.teamcode.replay.

ACCEPTANCE CRITERIA

- Every constant in spec §9 and §9.1 has a corresponding field in this
  file, same name, same value.
- File compiles standalone (no dependency on classes that don't exist
  yet — ReplaySample, ReplayRecorder, etc. must not be referenced).
- A reviewer can diff this file's name→value pairs against spec §9/§9.1
  line by line with no mismatches.

TESTS YOU MUST PERFORM

- Self-check: list every constant name+value you wrote next to the
  corresponding spec table entry, confirm they match exactly (this is
  the "extract name→value pairs" step directory-structure.md §3.2 will
  later use for the Java/Kotlin diff — get it right now so that check is
  trivial later).

REQUIRED FINAL REPORT

- Path of the file created.
- Full list of constants written, each paired with its spec section
  (§9 or §9.1) for traceability.
- Confirmation the file has no logic beyond the constructor guard.
- Confirmation the self-check found no mismatches against the spec.
